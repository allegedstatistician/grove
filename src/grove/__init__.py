"""grove â€” a git worktree and checkpoint manager.

This package is a learning project. Public surface is deliberately small:
the CLI in `grove.cli` is the only intended entry point.
"""

__version__ = "0.1.0"
